numpy>=1.26.4
pandas>=2.1.4
scikit-learn>=1.4.0
xgboost>=2.0.3
pyyaml>=6.0.1
joblib>=1.3.2
# duckdb: >=1.0 required — the create_function API and EXCLUDE/REPLACE syntax
# used in the pipeline changed in 1.0 from the original 0.10.x pin.
duckdb>=1.0.0
pyspark>=3.5.0
pyarrow>=15.0.0
matplotlib>=3.8.0
seaborn>=0.12.0
notebook>=7.0.7
ipykernel>=6.29.0
jupyterlab>=4.1.0
# Graph features (--pagerank flag)
# networkx is the fallback PageRank engine when Spark/GraphFrames is unavailable.
networkx>=3.0
# graphframes is the Spark GraphFrames Python wrapper. It cannot be installed
# via pip in all environments — install it manually into the correct conda env:
#   /path/to/conda/envs/<env>/bin/pip install graphframes
# The pipeline falls back to networkx automatically if graphframes is missing.
